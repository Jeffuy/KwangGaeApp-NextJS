import React from 'react';
import MarketMain from '@components/market/MarketMain';

const MarketContainer = ({ cardList }) => {
	return <MarketMain cardList={cardList} />;
};

export default MarketContainer;
